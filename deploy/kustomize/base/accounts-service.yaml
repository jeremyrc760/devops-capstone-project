apiVersion: v1
kind: Service
metadata:
  name: accounts
  namespace: accounts
  labels:
    app.kubernetes.io/name: accounts
    app.kubernetes.io/part-of: devops-capstone
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: accounts
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 32080
