apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      version: v1
      kind: Service
      name: accounts
      namespace: accounts
    path: accounts-service-clusterip-patch.yaml
